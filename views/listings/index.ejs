<% layout("layouts/boilerplate")%>

<style>
/* Container holding filters + toggle */
#filters-container {
  display: flex;
  align-items: center;
  width: 100%;
  overflow: hidden; /* Prevents page overflow */
  padding: 1rem 0;
  gap: 1rem;
}

/* Filters row */
#filters {
  display: flex;
  overflow-x: auto; /* Scrollable horizontally */
  white-space: nowrap;
  flex: 1; /* Takes remaining space */
  scrollbar-width: thin; /* Firefox */
  gap: 1rem;
}

/* Individual filter */
.filter {
  flex: 0 0 auto; /* Prevent shrinking */
  text-align: center;
  opacity: 0.5;
  cursor: pointer;
}

.filter:hover {
  opacity: 1;
  color: #fe424d;
}

.filter p {
  font-size: 0.7rem;
  margin-top: 0.2rem;
}

/* Toggle stays fixed */
.tax-toggle {
  flex-shrink: 0; /* Stay fixed */
  white-space: nowrap;
}

/* Optional: Customize scrollbar */
#filters::-webkit-scrollbar {
  height: 6px;
}
#filters::-webkit-scrollbar-thumb {
  background: #ccc;
  border-radius: 3px;
}

.taxInfo {
  display: none;
}

.tax-toggle{
border:  1px solid black;
border-radius: 1rem;
padding: 0.5rem;
margin-bottom: 2rem;
}
</style>

<!-- Filters + toggle container -->
<div id="filters-container">
  <div id="filters">
    <a href="/listings?category=Trending" class="filter"><i class="fa-solid fa-fire"></i><p>Trending</p></a>
    <a href="/listings?category=Rooms" class="filter"><i class="fa-solid fa-bed"></i><p>Rooms</p></a>
    <a href="/listings?category=Iconic Cities" class="filter"><i class="fa-solid fa-gopuram"></i><p>Iconic Cities</p></a>
    <a href="/listings?category=pools" class="filter"><i class="fa-solid fa-person-swimming"></i><p>Pools</p></a>
    <a href="/listings?category=Beaches" class="filter"><i class="fa-solid fa-water"></i><p>Beaches</p></a>
    <a href="/listings?category=Mountains" class="filter"><i class="fa-solid fa-mountain-sun"></i><p>Mountains</p></a>
    <a href="/listings?category=Castles" class="filter"><i class="fa-brands fa-fort-awesome"></i><p>Castles</p></a>
    <a href="/listings?category=Camping" class="filter"><i class="fa-solid fa-campground"></i><p>Camping</p></a>
    <a href="/listings?category=Farms" class="filter"><i class="fa-solid fa-cow"></i><p>Farms</p></a>
    <a href="/listings?category=Arctic" class="filter"><i class="fa-solid fa-snowflake"></i><p>Arctic</p></a>
    <a href="/listings?category=Boat" class="filter"><i class="fa-solid fa-sailboat"></i><p>Boat</p></a>
    <a href="/listings?category=Dome" class="filter"><i class="fa-solid fa-igloo"></i><p>Dome</p></a>
    <a href="/listings?category=Golfing" class="filter"><i class="fa-solid fa-golf-ball-tee"></i><p>Golfing</p></a>
    <a href="/listings?category=City" class="filter"><i class="fa-solid fa-city"></i><p>City</p></a>
    <a href="/listings?category=Lakes" class="filter"><i class="fa-solid fa-water"></i><p>Lakes</p></a>
  </div>

  <!-- Fixed toggle button -->
  <div class="tax-toggle">
    <div class="form-check-reverse form-switch">
      <input class="form-check-input" type="checkbox" role="switch" id="switchCheckDefault">
      <label class="form-check-label" for="switchCheckDefault"> Display total after taxes</label>
    </div>
  </div>
</div>

<!-- Listings -->
<div class="row row-cols-lg-4 row-cols-md-2 row-cols-sm-1 mt-3">
  <% for(let listing of allListings) { %>
    <a href="listings/<%= listing._id %>" class="listing-link">
      <div class="card col listing-card">
        <img src="<%= listing.image.url %>" class="card-img-top" alt="listing_img" style="height: 20rem">
        <div class="card-img-overlay"></div>
        <div class="card-body">
          <p class="card-text">
            <b><%= listing.title %></b><br>
            &#8377; <%= Number(listing.price).toLocaleString("en-IN") %>/night
            <i class="taxInfo"> +18% GST</i>
          </p>
        </div>
      </div>
    </a>
  <% } %>
</div>

<script>
document.addEventListener("DOMContentLoaded", () => {
  let taxSwitch = document.getElementById("switchCheckDefault");
  if (taxSwitch) {
    taxSwitch.addEventListener("click", () => {
      let taxInfo = document.getElementsByClassName("taxInfo");
      for(let info of taxInfo){
        if(info.style.display !=="inline"){
        info.style.display= "inline";
        } else{
          info.style.display= "none";
        }
      }
    });
  }
});
</script>
